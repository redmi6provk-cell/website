import { PageLoader } from "@/components/ui/PageLoader";

export default function Loading() {
  return (
    <PageLoader
      title="Loading FMCG Store"
      subtitle="Truck aa raha hai, page bas abhi ready hota hai."
    />
  );
}
